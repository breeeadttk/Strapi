'use strict';

/**
 * recommended-action router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::recommended-action.recommended-action');
